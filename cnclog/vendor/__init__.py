"""Third-party code bundled with the program.

Nothing here is our own work; each subpackage keeps its original licence file.

Bundled rather than pip-installed because the target is a HEROS control PC
where SELinux blocks package installation and there is no pip. Copying the
folder has to be the whole install.

  pyLSV2 1.2 — MIT — https://github.com/drunsinn/pyLSV2
      Pure-Python LSV2 client, no dependencies of its own.
"""
